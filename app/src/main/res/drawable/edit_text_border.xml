<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent"/> <!-- White background color -->
    <corners android:radius="4dp"/> <!-- Adjust radius for curved corners -->
    <stroke
        android:width="1dp"
        android:color="@color/white"/> <!-- Border color -->
</shape>